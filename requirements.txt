# core decoder + synthetic testbed
torch>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.6
pytest>=7.0

# real-EEG benchmark, baselines, and the statistical harness
mne>=1.6          # PhysioNet EEGMMI loading + CSP
scikit-learn>=1.2 # CSP+LDA / Ridge baselines, cross-validation
statsmodels>=0.14 # power analysis, mixed models, ICC
pandas>=2.0       # mixed-model data frames

# optional — only for the intracortical benchmark (scripts/run_intracortical_benchmark.py)
# pynwb>=2.5      # read MC_Maze NWB directly (nlb_tools is NOT used: it pins pandas<=1.3.4)
